{
  "name": "repo-tracker",
  "version": "1.0.0",
  "description": "Self-updating GitHub repo index + profile dashboard via GitHub Actions.",
  "main": "scripts/sync.js",
  "scripts": {
    "sync": "node scripts/sync.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
